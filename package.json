{
  "name": "orderly_stake",
  "version": "1.0.0",
  "license": "(MIT AND Apache-2.0)",
  "scripts": {
    "deploy": "cd contracts/order-stake && ./deploy.sh",
    "build": "npm run build:order-stake",
    "build:order-stake": "cd contracts/order-stake && ./build.sh",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "cd contracts/order-stake && cargo test",
    "test:integration": "cd integration-tests && cargo run --example integration-tests \"../target/wasm32-unknown-unknown/release/order_stake.wasm\"",
    "postinstall": "echo no frontend && echo rs tests && echo rs order-stake"
  },
  "devDependencies": {
    "near-cli": "^3.3.0"
  },
  "dependencies": {}
}